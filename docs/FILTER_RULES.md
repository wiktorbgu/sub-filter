[EN](FILTER_RULES_en.md) / [RU](FILTER_RULES.md) / [ZH](FILTER_RULES_zh.md)

- [Документация для `rules.yaml`](#документация-для-rulesyaml)
  - [Структура файла](#структура-файла)
  - [1. `required_params` — Обязательные параметры](#1-required_params--обязательные-параметры)
  - [2. `allowed_values` — Разрешённые значения](#2-allowed_values--разрешённые-значения)
  - [3. `forbidden_values` — Запрещённые значения](#3-forbidden_values--запрещённые-значения)
  - [4. `conditional` — Условные правила](#4-conditional--условные-правила)
  - [Важные изменения (февраль 2026)](#важные-изменения-февраль-2026)
  - [Актуальный пример для VLESS (февраль 2026)](#актуальный-пример-для-vless-февраль-2026)
  - [Особенности валидации](#особенности-валидации)

## Документация для `rules.yaml`

Файл `rules.yaml` — это **набор правил** для программы `sub-filter`. Эти правила определяют, какие прокси-ссылки считаются **корректными и безопасными**, а какие — **недопустимыми** и подлежат удалению.

Представьте, что `sub-filter` — это **умный фильтр для воды**, а `rules.yaml` — его **инструкция**: какие примеси удалять, а какую чистую воду пропускать.

---

### Структура файла

Файл разделён на **секции по протоколам**. Поддерживаемые протоколы:

- `vless`
- `vmess`
- `trojan`
- `hysteria2`
- `ss` (Shadowsocks)

В каждой секции могут присутствовать **четыре типа правил**:

---

### 1. `required_params` — Обязательные параметры

Список параметров, которые **обязательно должны присутствовать** в ссылке.

- Если хотя бы одного параметра **нет** — ссылка отклоняется.
- **Пример для VLESS:**
  ```yaml
  required_params: [encryption, sni]
  ```

---

### 2. `allowed_values` — Разрешённые значения

Список **допустимых значений** для конкретного параметра.

- Если значение параметра **не входит** в список — ссылка отклоняется.
- **Пример:**
  ```yaml
  allowed_values:
    security: [tls, reality]
    type: [tcp, ws, httpupgrade, grpc, xhttp]
    flow:
      - 'xtls-rprx-vision'
      - 'xtls-rprx-vision-udp443'
  ```

> ⚠️ Правило применяется **только если параметр присутствует**. Отсутствующие параметры проверяются через `required_params`.

---

### 3. `forbidden_values` — Запрещённые значения

Список **запрещённых значений** для конкретного параметра.

- Если значение **входит** в список — ссылка отклоняется.
- **Имеет приоритет над `allowed_values`**.
- **Пример:**
  ```yaml
  forbidden_values:
    security: [none]
    authority: [""]  # запрет пустого authority
  ```

> ⚠️ Это правило **глобально запрещает** указанные значения во всех случаях.  
> Чтобы разрешить исключение (например, `security=none` только для `type=ws`), используйте **условные правила**.

---

### 4. `conditional` — Условные правила

Правила, которые применяются **только при выполнении определённых условий**.

Структура:
- `when` — условия активации (все должны быть истинны)
- `require` — обязательные параметры (если условие выполнено)
- `forbidden_values` — запрещённые значения (если условие выполнено)

**Примеры:**
```yaml
conditional:
  # Если security=reality, обязательно должен быть pbk
  - when: { security: 'reality' }
    require: [pbk]

  # Если type=grpc, обязательно должен быть serviceName
  - when: { type: 'grpc' }
    require: [serviceName]

  # Если type НЕ ws, запрещать security=none
  - when: { type: { not: 'ws' } }
    forbidden_values: { security: ['none'] }
```

---

### Важные изменения (февраль 2026)

**🔴 Trojan: Параметр `flow` больше не поддерживается**

В Xray-core 2024+ параметр `flow` был **удалён** из протокола Trojan. Программа `sub-filter` теперь **отклоняет** конфиги Trojan с параметром `flow`:

```yaml
trojan:
  required_params:
    - password
  allowed_values:
    type: [tcp, ws, grpc, httpupgrade, xhttp, splithttp]
    security: [tls, reality]
    # ❌ flow НЕ включен в allowed_values (удалён в Xray-core 2024+)
    # Любой Trojan с параметром flow будет отклонен:
    # "trojan://password@example.com?flow=xtls-rprx-vision" ← отклонено
```

**Что делать, если у вас есть Trojan с `flow`?**
- Обновите конфиг: удалите параметр `flow` из URL
- Пример: `trojan://password@example.com:443` (без `?flow=...`)

---

**🔴 Shadowsocks: CFB и CTR методы больше не поддерживаются**

В Xray-core 2024+ **удалены** методы шифрования CFB и CTR (устаревшие потоковые шифры). Поддерживаются **только современные методы**:

✅ **AEAD методы** (современный стандарт):
- `aes-128-gcm`
- `aes-256-gcm`
- `chacha20-poly1305`
- `xchacha20-poly1305`

✅ **Shadowsocks 2022** (новый стандарт с Blake3):
- `2022-blake3-aes-128-gcm`
- `2022-blake3-aes-256-gcm`
- `2022-blake3-chacha20-poly1305`

❌ **Больше не поддерживаются**:
- `aes-128-cfb` (удалён)
- `aes-256-cfb` (удалён)
- `aes-128-ctr` (удалён)
- `aes-256-ctr` (удалён)

```yaml
ss:
  required_params:
    - password
    - method
  allowed_values:
    method:
      - 'aes-128-gcm'          # ✅ AEAD
      - 'aes-256-gcm'          # ✅ AEAD
      - 'chacha20-poly1305'    # ✅ AEAD
      - 'xchacha20-poly1305'   # ✅ AEAD
      - 'none'                 # ✅ Без шифрования
      - '2022-blake3-aes-128-gcm'      # ✅ SS 2022
      - '2022-blake3-aes-256-gcm'      # ✅ SS 2022
      - '2022-blake3-chacha20-poly1305' # ✅ SS 2022
      # ❌ CFB и CTR методы удалены (не реализованы в Xray-core)
```

**Что делать, если у вас есть Shadowsocks с CFB/CTR?**
- Обновите метод шифрования на один из поддерживаемых (например, `aes-256-gcm`)
- Или обновите на `2022-blake3-aes-256-gcm` (более современный стандарт)
- Пример: `ss://aes-256-gcm:password@example.com:8388` (вместо `aes-256-cfb`)

---

### Актуальный пример для VLESS (февраль 2026)

```yaml
vless:
  required_params:
    - encryption
    - sni
  forbidden_values:
    security: [none]
    authority: [""]  # блокировка пустого authority (распространённая ошибка в gRPC)
  allowed_values:
    security: [tls, reality]
    type: [tcp, ws, httpupgrade, grpc, xhttp]  # явное перечисление всех допустимых транспортов
    flow:
      - 'xtls-rprx-vision'
      - 'xtls-rprx-vision-udp443'
      - 'xtls-rprx-vision-direct'
    mode: [gun, multi]  # официальные режимы для gRPC
  conditional:
    - when: { security: 'reality' }
      require: [pbk]

    - when: { type: 'grpc' }
      require: [serviceName]

    - when: { type: 'ws' }
      require: [path]

    - when: { type: 'httpupgrade' }
      require: [path]

    - when: { type: 'xhttp' }
      require: [path]

    - when: { type: 'xhttp', mode: 'packet' }
      require: []
```

**Объяснение:**

1. **Во всех VLESS-ссылках** обязательно должны быть `encryption` и `sni`.
2. Параметр `security` может быть только `tls` или `reality`; `none` глобально запрещён.
3. Явно разрешены только известные и поддерживаемые транспорты (`type`).
4. Для `security=reality` обязателен `pbk`.
5. Для `type=grpc` обязателен `serviceName`.
6. Для всех HTTP-подобных транспортов (`ws`, `httpupgrade`, `xhttp`) обязателен `path`.
7. Пустой параметр `authority=` (частая ошибка в gRPC) автоматически отфильтровывается.
8. Поддержка `gRPC + REALITY` теперь официальна — правила это корректно отражают.

---

### Особенности валидации

- Для VLESS, если параметр `security` **отсутствует**, он **автоматически считается равным `none`**.
- Правила применяются **только к присутствующим параметрам**.
- Порядок проверки:  
  `forbidden_values` → `allowed_values` → `conditional`
- Все сравнения значений — **регистрозависимые** (используйте точные значения из спецификаций).
- **Недопустимые значения `type` (например, `raw`, `h2`, `kcp`) будут отклонены**, если они не указаны в `allowed_values.type`.
- Параметр `mode` имеет смысл **только для `type=grpc`**, но валидатор проверяет его значение глобально — поэтому важно указывать только официальные режимы (`gun`, `multi`).

---

> 💡 **Рекомендация**: всегда явно указывайте `allowed_values.type`. Это защищает от ошибок генераторов подписок и обеспечивает совместимость с современными клиентами (Xray-core, Sing-box, Mihomo).