- [sub-filter](#sub-filter)
  - [Что умеет программа?](#что-умеет-программа)
  - [Как собрать программу?](#как-собрать-программу)
  - [Как запустить?](#как-запустить)
    - [Пример с полными настройками](#пример-с-полными-настройками)
    - [Пример с минимальными настройками](#пример-с-минимальными-настройками)
  - [Что означают параметры?](#что-означают-параметры)
  - [Как проверить, что всё работает?](#как-проверить-что-всё-работает)
    - [Как использовать в клиенте?](#как-использовать-в-клиенте)
  - [Как собрать Docker-образ?](#как-собрать-docker-образ)
  - [Как запустить в Docker?](#как-запустить-в-docker)
    - [Через Docker](#через-docker)
    - [Через Podman (альтернатива Docker)](#через-podman-альтернатива-docker)

# sub-filter

Простой фильтр подписок

Эта программа — умный фильтр-прослойка для ссылок на прокси-серверы (VLESS, VMess, Trojan, Shadowsocks). Она берёт в реальном времени публичные подписки, проверяет каждый сервер на корректность и безопасность (например, блокирует незашифрованные подключения или названия с запрещёнными словами), отсеивает всё подозрительное и выдаёт чистый, рабочий список — готовый к использованию в Clash, Sing-Box, роутерах и других клиентах.

Если у вас есть вопросы о том, зачем это нужно — прочитайте [FAQ](FAQ.md).

 ⚠️  Программа не проверяет ЖИВУЧЕСТЬ проксей, для этого используйте https://github.com/kutovoys/xray-checker

---

## Что умеет программа?

✅ Проверяет ссылки на корректность и удаляет небезопасные или сломанные конфигурации  
✅ Фильтрует серверы по списку запрещённых слов (например, подозрительные домены)  
✅ Блокирует известные «ловушки» (ханипоты), которые иногда попадаются в публичных подписках  
✅ Кеширует результаты (по умолчанию — 30 минут), чтобы не перегружать сеть и серверы  
✅ Формирует подписку с понятным описанием и корректным форматом

---

## Как собрать программу?

Если у вас установлен Go (версия 1.21 или новее), выполните в терминале:

```
go build -o filter .
```

После этого появится файл `filter` — это и есть ваша программа.

---

## Как запустить?

### Пример с полными настройками

```
./filter 8000 1800 ./config/sub.txt ./config/bad.txt ./config/uagent.txt
```

### Пример с минимальными настройками

```
./filter 8000 1800
```

(В этом случае программа будет искать файлы в папке `./config/`)

---

## Что означают параметры?

| Параметр     | Описание                                                                               |
| ------------ | -------------------------------------------------------------------------------------- |
| `8000`       | Порт, на котором будет работать сервер (можно изменить)                                |
| `1800`       | Время хранения кеша в секундах (1800 = 30 минут)                                       |
| `sub.txt`    | Список URL-подписок (по одному на строку)                                              |
| `bad.txt`    | Слова, которые нельзя включать в подписку (например, подозрительные домены)            |
| `uagent.txt` | Какие клиенты (User-Agent) могут получать подписку (например, `Clash`, `Shadowrocket`) |

> 💡 Если вы не укажете пути к файлам, программа будет искать их в папке `./config/`.

---

## Как проверить, что всё работает?

Попробуйте запросить отфильтрованную подписку из первой строки файла `sub.txt`:

```
curl -H "User-Agent: Clash" "http://localhost:8000/filter?id=1"
```

Если всё настроено правильно — вы увидите «чистую» подписку.

> 💡 Совет: убедитесь, что название вашего клиента (например, `Clash`) есть в файле `uagent.txt` — так подписка точно загрузится.

### Как использовать в клиенте?

Добавьте в настройки клиента подписку вида:

```
http://сервер:порт/filter?id=номер
```

Замените:
- `сервер` → IP-адрес вашего роутера, Raspberry Pi или сервера  
- `порт` → порт, который вы указали при запуске (например, `8000`)  
- `номер` → номер строки из файла `sub.txt` с нужной подпиской (первая строка = `id=1`)

> 🔒 Рекомендуется запускать программу за reverse proxy с HTTPS (например, Nginx, Caddy или Cloudflare), особенно если доступ возможен из интернета.

---

## Как собрать Docker-образ?

```
docker build -t sub-filter .
```

---

## Как запустить в Docker?

### Через Docker

```
docker run -d \
  --name sub-filter \
  -p 8000:8000 \
  -v $(pwd)/config:/config:ro \
  -v $(pwd)/cache:/cache:rw \
  sub-filter \
  8000 1800
```

### Через Podman (альтернатива Docker)

```
podman run -d --replace \
  --name sub-filter \
  -p 8000:8000 \
  -v $(pwd)/config:/config:ro,z \
  -v $(pwd)/cache:/cache:rw,z \
  sub-filter \
  8000 1800
```

> 📁 Убедитесь, что папки `./config` и `./cache` существуют перед запуском:
> ```
> mkdir -p ./config ./cache
> ```


